{
  "name": "agent-pod-cli",
  "version": "1.15.0",
  "description": "Run AI coding-agent CLIs inside a Docker sandbox scoped to the current project.",
  "author": "Jake Chase-Lubitz",
  "bin": {
    "agent-pod": "agent-pod"
  },
  "scripts": {
    "postinstall": "node scripts/postinstall.js",
    "pack:check": "npm pack --dry-run",
    "test": "bash -n agent-pod install.sh uninstall.sh scripts/check-local-global-install.sh && scripts/check-local-global-install.sh"
  },
  "keywords": [
    "agent",
    "codex",
    "claude",
    "cursor",
    "docker",
    "sandbox"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
